package util;

public class ProductInfoValidator {
	public boolean nameValidator(String name) {
		// 상품 이름 규칙에 맞는지 확인
		
		// 맞지 않다 -> true 반환
		
		// 맞다 -> false 반환
		return false;
	}
	
	public boolean categoryValidator(String category) {
		// 상품 카테고리 규칙에 맞는지 확인
		
		// 맞지 않다 -> true 반환
		
		// 맞다 -> false 반환
		return false;
	}
	
	public boolean stockValidator(int stock) {
		// 상품 재고 규칙에 맞는지 확인
		
		// 맞지 않다 -> true 반환
		
		// 맞다 -> false 반환
		return false;
	}
	
	public boolean priceValidator(int stock) {
		// 상품 가격 규칙에 맞는지 확인
		
		// 맞지 않다 -> true 반환
		
		// 맞다 -> false 반환
		return false;
	}
	
	public boolean imgValidator(String img) {
		// 상품 이미지 규칙에 맞는지 확인
		
		// 맞지 않다 -> true 반환
		
		// 맞다 -> false 반환
		return false;
	}
}











